###This configuration does not support comments after parameters and must be started on a new line.
###Do not allow extra spaces between options and values

showwindowed=yes
# -- Display OSC when windowed, default: yes
showfullscreen=yes
# -- Display OSC in full screen, default: yes
#idlescreen=no
# -- Display the logo icon and text below when the player is idle
#scalewindowed=0.75
# -- OSC scaling factor when windowing, default: 1
#scalefullscreen=1.2
# -- OSC scaling factor in full screen, default: 1
#scaleforcedwindow=1
# -- Force the scaling factor of the (virtual) window? ? Default: 2 --Scale factor of the OSC when rendered on a forced (dummy) window
#vidscale=no
# -- Scale OSC with video size? Default: yes
valign=0.8
# -- <-1(Top)--1(Bottom)> Vertical alignment Default: 0.8
halign=0
# -- <-1(leftmost)--1(rightmost)> Horizontal alignment Default: 0
barmargin=0
# -- Vertical margin of bottom/top bar when using "bottombar/topbar" layout, default: 0
boxalpha=80
# -- <0(opaque)-255(fully transparent)> The overall transparency of the box element, default: 80

hidetimeout=500
# -- The duration (milliseconds) that OSC is hidden if the mouse is not moving. Non-negative number, default: 500
fadeduration=200
# -- Fade out duration (milliseconds) Default: 200, no fade out animation is 0
deadzonesize=1
# -- <0.0--1.0> Control the interaction between the cursor and the OSC area. Default: 0.5
minmousemove=0
# -- The minimum number of pixels the mouse must move between ticks to display OSC, default: 0
iamaprogrammer=no
# -- Use native mpv values ​​and disable OSC internal track list management (and some features that rely on it), default: no

layout=bottombox
# -- <box|slimbox|default bottombar|topbar> Select the built-in layout scheme
# -- "bottombox" is a new exclusive layout of Thumbnailer_OSC, which is improved based on the box layout and is compatible with thumbnail scripts
# -- This layout does not support valign halign boxalpha these options that originally affect the box layout
# -- If you use the title length of the example below, the window aspect ratio needs to be at least greater than 1.3 to prevent the title column from being covered.
# -- This layout may cause unknown freezes or even window freezes caused by non-performance reasons (solution: exclude potentially affecting parameters in mpv.conf, or change the playback progress)

seekbarstyle=knob
# -- <default bar|diamond|knob> select progress bar style
seekbarhandlesize=0.6
# -- The size of diamond and knob (relative to the full height of seekbar)
seekrangestyle=inverted
# -- <bar|line|slider|Default inverted|none> The style of the search range (when --seekbarstyle=bar, the slider of this item is an invalid value)
seekrangeseparate=yes
# -- [Effective when --seekbarstyle=bar] Whether to separate seekrange from the progress bar, default: yes
seekrangealpha=200
# -- <0-255> Transparency of seekrange, default: 200
seekbarkeyframes=yes
# -- Control the search mode when dragging the progress bar. If set to yes, the default search mode is used (usually keyframes but depends on the MPV priority setting). If set to no, exact search is used (slower). Default: yes


title=${!playlist-count==1:List:${playlist-pos-1}/${playlist-count}} ${!chapters==0:Chapter:${chapter}/${chapters}} ${!speed==1.00:Speed:${speed}} ${?percent-pos:Progress:${percent-pos}%} Volume:${volume} ${?video-format:encoding:${video-format}} ${?video-params/pixelformat:${video-params/pixelformat}} decoding:${?hwdec-current==no:SW}${ ?=hwdec-current==:SW}${!hwdec-current==no:${hwdec-current}} ${?colormatrix-primaries:Color gamut:${colormatrix-primaries}}
# -- A string compatible with attribute expansion, displayed as an OSC title (note that if --no-boder in the mpv.conf configuration is enabled, this item will also be displayed as a borderless title)
# -- The main title display content of all layouts is compatible with the property https://mpv.io/manual/master/#property-list expansion string https://mpv.io/manual/master/#property-expansion
##This item will also affect --no-boder, the upper title in borderless mode. The implementation of OSC is essentially an ASS subtitle. The more variables are monitored, the slower the response speed. The most intuitive manifestation is the change in feedback perception when the mouse moves to view the timeline.

tooltipborder=1
# --border of tooltip in bottom/topbar layout --border of tooltip in bottom/topbar
timetotal=yes
# -- Display total time instead of remaining time, default: no
timems=no
# -- Display timecode with millisecond precision, default: no
visibility=auto
# -- <never|default auto|always> OSC visibility

boxmaxchars=150
# -- The upper limit of the number of title characters in the "box" layout, default: 150
boxvideo=no
# -- Affects the layout between OSC and video, see https://mpv.io/manual/master/#on-screen-controller-boxvideo for details
windowcontrols=auto
windowcontrols_alignment=right
# -- <left|Default right> The above two items are related to window controls in borderless mode https://mpv.io/manual/master/#on-screen-controller-windowcontrols
greenandgrumpy=no
# -- Whether to turn off holiday eggs, default: no
livemarkers=yes
# -- Update chapter markers immediately, default: yes
#chapters_osd=no
# -- Display all OSD chapter information when left-clicking to switch chapters. Default: yes
playlist_osd=no
# -- Display OSD playlist information when left-clicking to switch files, default: yes (OSD will overlap when playing new files)
#chapter_fmt=Chapter: %s
# -- The format for displaying chapter information when hovering the progress bar. The special value no means not to display. The example is the default value.

##The following parameters do not exist in the original OSC
wctitle=${?pause==yes:⏸}${?mute==yes:🔇}${?ontop==yes:📌}${?demuxer-via-network==yes:${media-title} }${?demuxer-via-network==no:${filename}} ${!playlist-count==1:list:${playlist-pos-1}/${playlist-count}} ${!chapters==0:Chapter:${chapter}} ${?percent-pos:Progress:${percent-pos}%}
# -- The display content of the upper title in borderless mode and the OSC title are independent of each other. The default is media-title.
#sub_title=
# -- The right subtitle of the bottombox layout (optional, left blank by default), compatible attributes and extended strings
sub_title2=${!contrast==0:Contrast[${contrast}]} ${!brightness==0:Brightness[${brightness}]} ${!gamma==0:Gamma[${gamma}] } ${!gamma-factor==1.000:Gamma coefficient[${gamma-factor}]} ${!saturation==0:Purity[${saturation}]} ${!hue==0:Hue[${hue}]} ${!speed==1.00:Speed[${speed}]} ${!fullscreen==yes:Scale[${current-window-scale} ]}
# -- Temporary right subtitle of the bottombox layout (forced to appear when the cursor moves to the timeline, defaults to monitoring palette properties), compatible properties and extended strings
seekbar_scrollseek=second
# -- When the cursor is hovering over the progress bar, slide the mouse wheel to trigger a jump mode. It is not controlled by the --hr-seek parameter in mpv.conf
# -- Available values: (default) fast is a keyframe, second is an exact second, frame is a frame (forced pause)
# -- Note: The speed of the three modes is in descending order, and the performance requirements are in ascending order. (Especially noticeable when jumping forward)
#showonpause=yes
# -- Permanent OSC on pause
#showonstart=yes
# -- Display OSC at the beginning of playback or when playing the next file
#showonseek=yes
# -- Display OSC when jumping

# -- The following three items are the global font display of OSC. The default values ​​are sans sans 500.
font=NotoSansCJKsc-Bold
font_mono=
font_bold=400