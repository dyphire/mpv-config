# 当前位置的显示样式。可用：line, bar
timeline_style=bar
# Line 显示样式配置
timeline_line_width=2
timeline_line_width_fullscreen=3
# 最小化时的缩放线宽 (timeline_size_min)
timeline_line_width_minimized_scale=10
# 最小化时的时间轴大小，0将完全隐藏它
timeline_size_min=2
# 窗口下完全展开时的时间轴大小，以像素为单位，0 表示禁用
timeline_size_max=30
# 与上述选项类似，但作用于全屏时
timeline_size_min_fullscreen=0
timeline_size_max_fullscreen=50
# 启动时是否隐藏时间轴
timeline_start_hidden=no
# 设置时间轴应始终可见的状态。使用逗号分隔，可用：paused, audio, image, video
timeline_persistency=
# 时间轴不透明度
timeline_opacity=0.8
# 背景颜色的顶部边框，有助于在视觉上将时间轴与视频分开
timeline_border=1
# 在时间轴上方滚动时，滚轮将搜索此秒数
timeline_step=5
# 显示流视频的可搜索缓冲范围，语法: `color:opacity`,
# 颜色是 BBGGRR 十六进制代码，设置为`none`以禁用
timeline_cached_ranges=4e845c:0.5
# 指定时间轴上的章节样式：never, dots, lines, lines-top, lines-bottom
timeline_chapters=lines-top
timeline_chapters_opacity=0.5
timeline_chapters_width=3

# 以逗号分隔的项列表，用于构造时间轴上方的控制栏。设置为`never`以禁用
# 参数规范：括在`{}`中表示值，括在`[]`中表示可选
# 完整项语法：`[<[!]{disposition1}[,[!]{dispositionN}]>]element[:paramN][?{tooltip}]`
# 常用属性：
#   `{icon}` - parameter used to specify an icon name (example: `face`)
#            - you can pick one here: https://fonts.google.com/icons?selected=Material+Icons
# Available elements and their parameters:
#   `{usoc_command}` - preconfigured shorthands for uosc commands that make sense to have as buttons:
#      - `menu`, `subtitles`, `audio`, `video`, `playlist`, `chapters`, `stream-quality`,
#        `open-file`, `items`, `next`, `prev`, `first`, `last`, `audio-device`
#   `fullscreen` - toggle fullscreen
#   `loop-playlist` - button to toggle playlist looping
#   `loop-file` - button to toggle current file looping
#   `speed[:{scale}]` - display speed slider, [{scale}] - factor of controls_size, default: 1.3
#   `command:{icon}:{command}` - button that executes a {command} when pressed
#   `toggle:{icon}:{prop}` - button that toggles mpv property
#   `cycle:{default_icon}:{prop}:{value1}[={icon1}][!]/{valueN}[={iconN}][!]`
#     - button that cycles mpv property between values, each optionally having different icon and active flag
#     - presence of `!` at the end will style the button as active
#   `gap[:{scale}]` - display an empty gap, {scale} - factor of controls_size, default: 0.3
#   `space` - fills all available space between previous and next item, useful to align items to the right
#           - multiple spaces divide the available space among themselves, which can be used for centering
#   Example implementations of some of the shorthands:
#   - menu: `command:menu:script-binding uosc/menu`
#   - fullscreen: `cycle:fullscreen:fullscreen:no/yes=fullscreen_exit!`
#   - loop-playlist: `cycle:repeat:loop-playlist:no/inf!`
#   - `toggle:{icon}:{prop}`: `cycle:{icon}:{prop}:no/yes!`
# Item visibility control:
#   `<[!]{disposition1}[,[!]{dispositionN}]>` - optional prefix to control element's visibility
#   - `{disposition}` can be one of:
#     - `image` - true if current file is a single image
#     - `audio` - true for audio only files
#     - `video` - true for files with a video track
#     - `has_audio` - true for files with an audio track
#     - `has_sub` - true for files with an subtitle track
#     - `has_chapter` - true for files with chapter list
#     - `stream` - true if current file is read from a stream
#     - `has_playlist` - true if current playlist has 2 or more items in it
#   - prefix with `!` to negate the required disposition
#   Examples:
#     - `<stream>stream-quality` - show stream quality button only for streams
#     - `<has_audio,!audio>audio` - show audio tracks button for all files that have
#                                   an audio track, but are not exclusively audio only files
# Item tooltip:
#   Place `?Tooltip text` after the element config to give it a tooltip.
#   Example: `<stream>stream-quality?Stream quality`
controls=menu,open-file,gap,command:info:script-binding stats/display-stats-toggle?info,gap,<video>video,<has_audio,!audio>audio,<has_sub>subtitles,<has_chapter>chapters,<stream>stream-quality,space,speed,space,loop-playlist,loop-file,shuffle,gap,prev,items,next,gap,audio-device,gap:1,fullscreen
controls_size=32
controls_size_fullscreen=40
controls_margin=8
controls_spacing=2
controls_persistency=

# 显示音量控制的位置：none, left, right
# 设置音量控制条的样式
volume=right
volume_size=40
volume_size_fullscreen=52
volume_persistency=
volume_opacity=0.8
volume_border=1
volume_step=1

# 播放速度小部件：鼠标拖动或滚轮更改，单击以重置
speed_persistency=
speed_opacity=0.7
speed_step=0.1
speed_step_is_factor=no

# 控制所有菜单，如上下文菜单，字幕加载器/选择器等
menu_item_height=36
menu_item_height_fullscreen=50
menu_min_width=200
menu_min_width_fullscreen=300
#是否启用'wasd'组合键操控菜单
menu_wasd_navigation=no
#是否启用'hjkl'组合键操控菜单
menu_hjkl_navigation=no
menu_opacity=0.7
menu_parent_opacity=0.8

# 带有窗口控件和媒体标题的顶栏
# 可以是：never, no-border, always
top_bar=no-border
top_bar_size=40
top_bar_size_fullscreen=46
top_bar_persistency=
top_bar_controls=yes
top_bar_title=yes

# 在无边框模式下绘制窗口边框
window_border_size=1
window_border_opacity=0.8

# 指定UI的缩放比例，请参考显示器的HIDPI缩放比例
ui_scale=1
# 调整文字缩放以适合您的字体
font_scale=1
# 直接在视频顶部绘制时文本和图标的边框
text_border=1.2
# 点击时间短于此毫秒数时暂停视频，0 表示禁用
pause_on_click_shorter_than=0
# `flash-{element}`命令使用的闪存持续时间（以毫秒为单位）
flash_duration=1000
# 以像素为单位的距离，低于该像素的元素完全淡入/淡出
proximity_in=40
proximity_out=120
# BBGGRR - 蓝绿红十六进制颜色代码
color_foreground=ffffff
color_foreground_text=000000
color_background=000000
color_background_text=ffffff
# 是否在整个 UI 中仅使用粗体字体粗细
font_bold=no
# 是否显示总时间而非剩余时间
total_time=no
# 在时间戳中显示亚秒级分数，直至此精度
time_precision=0
# 是否在 mpv 自动隐藏光标时隐藏 UI
autohide=no
# 指定切换暂停状态时显示的指示器样式
# 可以是：none, flash, static, manual (由flash-pause-indicator和decide-pause-indicator命令控制)
pause_indicator=manual
# 指定当菜单打开时屏幕的变暗程度，0 禁用
curtain_opacity=0
# 指定在流质量菜单中列出的大小
stream_quality_options=4320,2160,1440,1080,720,480,360,240,144
# 是否在目录中的最后一个文件上调用下一个文件时加载第一个文件，反之亦然
directory_navigation_loops=no
# 指定导航媒体文件时要查找的文件类型
media_types=3gp,aac,avi,amv,bmp,bdmv,flac,f4v,flv,gif,h264,h265,ifo,iso,jpeg,jpg,m2ts,m4a,m4v,mid,midi,mkv,mov,mp3,mp4,mp4a,mp4v,mpeg,mpg,mka,dts,dtshd,dts-hd,truehd,true-hd,oga,ogg,ogm,ogv,opus,png,rm,rmvb,svg,ts,tif,tiff,vob,wav,weba,webm,webp,wma,wmv
# 指定加载外部字幕时要查找的文件类型
subtitle_types=aqt,etf,etf8,gsub,idx,jss,mks,pjs,psb,pgs,rt,sub,smi,slt,ssf,srt,ssa,ass,ttxt,usf,vt,vtt,utf-8
# 用于近似文本宽度。如果您使用的是一些宽字体，并且在菜单中看到很多右侧剪切，尝试增加它
font_height_to_letter_width_ratio=0.5
# 指定默认打开文件菜单目录
default_directory=C:/

# `chapter_ranges` 允许将章节指标转换为范围指标
#
# 章节范围定义语法：
# ```
# start_pattern<color:opacity>end_pattern
# ```
#
# 可以通过用`|`分隔来定义多个开始和结束模式：
# ```
# p1|pN<color:opacity>p1|pN
# ```
#
# 可以通过用逗号分隔来定义多个章节范围：
#
# chapter_ranges=range1,rangeN
#
# `start_pattern`之一可以是自定义关键字`{bof}`，当它有意义时它将匹配文件开头
#
# `end_pattern`之一可以是自定义关键字”{eof}`，当它有意义时它将匹配文件结尾
#
# 匹配模式是 lua 模式 （http://lua-users.org/wiki/PatternsTutorial）
# 它们只需要出现在标题中，而不是完全匹配它
# 匹配不区分大小写
#
# 结束范围并由`end_pattern`匹配的章节，而不是`.*`
# 不会在悬停时显示，除非该章节也用于启动另一章范围
#
# `color`是`bbggrr`十六进制颜色代码
# `opacity`是从 0 到 1 的浮点数，表示不透明度
#
# 示例：
#
# 将动画的开头和结尾显示为范围：
# ```
# chapter_ranges=^op| op$|opening<968638:0.5>.*, ^ed| ed$|^end|ending$<968638:0.5>.*|{eof}
# ```
#
# 显示可跳过的youtube视频赞助商块 https://github.com/po5/mpv_sponsorblock
# ```
# chapter_ranges=sponsor start<3535a5:.5>sponsor end, segment start<3535a5:0.5>segment end
# ```
chapter_ranges=^op |^op$| op$|opening$<968638:0.5>.*, ^ed |^ed$| ed$|ending$<968638:0.5>.*|{eof}, sponsor start<3535a5:.5>sponsor end, segment start<3535a5:0.5>segment end, ^sponsors?<3535a5:.5>.*, ^intro$<968638:0.5>.*, ^outro$<968638:0.5>.*|{eof}
